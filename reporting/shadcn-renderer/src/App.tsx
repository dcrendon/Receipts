export default function App() {
  return (
    <main
      style={{
        fontFamily: "Manrope, ui-sans-serif, system-ui, sans-serif",
        maxWidth: "48rem",
        margin: "2rem auto",
        padding: "1rem",
      }}
    >
      <h1>Shadcn Report Renderer</h1>
      <p>
        This Vite + shadcn package app is used by the Deno CLI to server-render
        HTML report output.
      </p>
    </main>
  );
}
